version: '3.8'
services:
  postgres:
    image: postgres:13
    environment:
      - POSTGRES_USER=hello
      - POSTGRES_PASSWORD=bro
      - POSTGRES_DB=iam
    ports:
      - "80:80"
    volumes:
  nginx:
    image: nginx:latest
    ports:
      - "90:90"
    volumes:
